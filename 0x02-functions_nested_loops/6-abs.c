#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: a program that calculates the absolute value of an integer
 *@int: argument
 * Return: return 0 on success
 */

int _abs(int)
{
	if (n < 0)
		n = -n;
			return (n);
}
