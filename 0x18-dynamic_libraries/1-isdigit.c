#include "main.h"

/**
 * _isdigit - Entry point of function
 *
 * Description: a program that check for a digit
 * @c: an integer argument
 *
 * Return: success
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
